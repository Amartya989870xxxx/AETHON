# Copy to .env.local and adjust when the backend moves off localhost.
# Everything in the app reads the base URL from this one variable.
VITE_API_BASE_URL=http://localhost:8000

# Operator identity sent as the `actor` / `added_by` audit field until real auth exists.
VITE_OPERATOR_NAME=demo_frontend
